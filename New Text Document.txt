if(rawText.length == 0){
    generatorCard()
  }
  else{
    main.value = ''
    const searchResult = arrayPokemons.find()
    if(searchResult){
      searchResult.map(pokemon => generatorSingleCard(pokemon))
    }else{
      main. innerHTML = '<p class="noresult">nada encontrado</p>'
    }
  }